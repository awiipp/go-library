services:
    redis:
        container_name: "go-library-redis"
        image: redis:8-alpine
        ports:
            - "6379:6379"
        volumes:
            - redis-data:/data

volumes:
    redis-data:
